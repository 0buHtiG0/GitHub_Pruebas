name: Deploy to server

on:
  push:
    branches: [develop]

jobs:
  second_job:
    runs-on: ubuntu-latest
    
    steps:
      - name: Hola Mundo
        run: echo "Hola Mundo"

  third_job:
    if: ${{ github.ref == 'refs/heads/develop'}}
    needs: second_job
    runs-on: ubuntu-latest
    
    steps:
      - name: Hola Mundo 2
        run: echo "Hola Mundo 2"
