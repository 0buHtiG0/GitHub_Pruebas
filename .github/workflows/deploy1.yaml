name: Deploy to server

on:
  push:
    branches: [develop]

jobs:
  second_job:
    runs-on: ubuntu-latest
    
    steps:
      - name: Hola Mundo
        run: cat hola.php

  third_job:
    needs: second_job
    runs-on: ubuntu-latest
    
    steps:
      - name: Hola Mundo 2
        run: echo "Hola Mundo 2"
